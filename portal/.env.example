# Optional backend service URLs for the Next.js portal.
# When unset, the portal falls back to the checked-in model metadata and dataset.

# Python FastAPI model service (Task 1)
PYTHON_API_URL=http://127.0.0.1:8000

# Java Spring Boot market analysis API (Task 2 - App 2)
JAVA_API_URL=http://127.0.0.1:8080
